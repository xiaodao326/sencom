spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://host.docker.internal:3306/sencom
    url: jdbc:mysql://localhost:3306/sencom
    username: root
    password: 123456

  data:
    redis:
#      host: host.docker.internal
      host: localhost
      port: 6379

mybatis:
  configuration:
    map-underscore-to-camel-case: true #开启驼峰 下划线 自动转换